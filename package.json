{
  "name": "coproq",
  "version": "0.1.3",
  "description": "message-passing coprocesses wrapped in OO syntax",
  "keywords": ["rpc", "process", "concurrent", "parallel", "worker", "node-coprocess", "message-passing"],
  "main": "coprocess.js",
  "files": ["README.md", "coprocess.js"],
  "license": "Apache-2.0",
  "author": "Andras",
  "repository": "https://github.com/andras/node-coprocess",

  "dependencies": {
    "qibl": "1.20.1"
  },

  "scripts": {
    "test": "qnit test-*",
    "coverage": "nyc -n coprocess.js --reporter lcov --reporter text npm test",
    "clean": "rm -rf .nyc_output coverage npm-debug.log"
  }
}
